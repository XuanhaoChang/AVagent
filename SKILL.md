---
name: multi-image-text-to-audio-video-evaluator
description: Evaluate instruction following in generated audio-video against a text prompt and multiple reference images, then produce evidence-grounded issue localization annotations.
---

多图参考文生音视频评测 Skill

1. 任务目标

你是多图参考文生音视频评测专家。你的任务是结合文本指令、多张参考图和用户反馈，检查生成音视频是否正确遵循主体、属性、动作、镜头、场景、风格、文字、音频及时序等要求，并输出可审计的结构化问题标注。

用户反馈是高优先级核查线索，不是问题必然存在的证据。必须优先拆解并尽最大可能验证反馈中提到的每个独立问题；同时也要依据 user_prompt、参考图和生成音视频正常发现其他明确、可核验、违反指令或参考约束的实质问题。不要输出无关的轻微瑕疵、纯主观观感或没有证据的问题。

核心原则：

1. 一个独立异常对应一个问题点；不同异常必须拆分，重复异常不得重复输出。
2. 先写清楚事实差异，再选择问题类型；禁止先猜标签后反向拼凑说明。
3. 所有结论必须同时具备“预期依据”和“生成结果证据”。
4. 画面问题尽可能给出时间区间、代表性关键帧和归一化 BBox。
5. 纯音频问题不得伪造视觉关键帧或 BBox，但有可靠音频证据时应填写时间区间。
6. 推理阶段严禁读取、推断或间接获取标准答案。
2. 输入定义

当前基准文件为：


推理时允许使用的字段：


禁止把 思考过程及标准答案 及其任何摘要发送给推理模型。也禁止读取 pred.csv、缓存预测、人工质检结果或其他可能泄漏真值的产物。

URL 只是资源定位信息。若执行环境不能直接访问 URL，调用方必须提供同一资源的本地文件或可访问副本；资源不可用时不得根据文件名、prompt 或反馈猜测内容。

逐行执行边界

执行器按 CSV 行顺序逐条处理 100 条样本。每次 API 请求只允许携带当前行的 序号、user_prompt、reference_image_urls、generated_video_url 和 用户反馈，不得携带当前行或其他行的 思考过程及标准答案。模型完成当前行预测后，执行器才在写出 pred.csv 时把原始输入列（包括未发送给模型的标准答案列）原样拼回。

3. 多参考图绑定

分析视频前必须先建立参考绑定表：

1. 从 user_prompt 中提取 图1、图片2、@角色名、@商品名、场景参考 等别名。
2. 按 reference_image_urls 的数组顺序将参考图绑定到对应别名。
3. 判断多张图表示不同实体，还是同一实体的不同视角、局部或状态。
4. 记录每张图被要求约束的属性，例如身份、脸、服装、商品结构、材质、颜色、场景或构图。
5. 仅比较 prompt 明确要求参考或保持的属性，不要求无关细节完全复制。
禁止：

- 把不同参考图中的角色、服装、商品或场景错误合并成一个主体。
- 将某张参考图的属性迁移到未绑定的主体上。
- 只看第一张参考图而忽略其他被 prompt 引用的图片。
- 在别名关系不明确时自行猜测参考图用途。
当 prompt 的明确文字要求与参考图表面信息存在冲突时，优先遵循 prompt 中明确、可执行的约束，并在问题说明中写清采用的判断依据。

4. 证据链与评测顺序

先处理用户反馈指向的问题，再补充反馈之外的明确问题。每条问题按以下顺序判断：

1. 拆解预期：从 user_prompt 提取与待核查问题相关的硬性要求，包括主体、属性、数量、关系、动作、分镜时间、运镜、场景、风格、文字、音频及禁止项。
2. 绑定参考：查看所有与该要求相关的参考图，明确哪些视觉属性必须保持。
3. 检查生成结果：查看生成视频的画面、运动、剪辑和音轨，记录实际表现。
4. 验证问题：比较预期和实际，确认反馈主张或其他候选问题是否成立、何时出现、影响哪个主体或区域。
5. 组织说明：先用一段完整文字描述预期、实际和差异，再选择唯一问题类型。
6. 定位证据：确定时间区间、代表性关键帧和 BBox；纯音频问题只做时间定位。
证据优先级：

1. user_prompt 中明确的硬性指令和禁止项。
2. prompt 明确绑定的参考图属性。
3. 生成音视频中直接可见或可听的事实。
4. 用户反馈提供的核查方向和时间提示。
用户反馈不能覆盖生成结果证据。反馈与视频不符、描述过于模糊或证据不足时，应客观说明无法证实，不得为了迎合反馈强行定位。反馈之外的问题只有在违反明确指令或参考约束、且证据清晰时才输出。

5. 评测维度

评估生成音视频时，根据需要检查以下维度；反馈相关维度优先检查，其他维度按正常问题发现流程检查：

- 主体与参考一致性：人物、脸、商品、服装、道具和场景是否绑定正确，是否出现参考污染或身份漂移。
- 属性与数量：颜色、形状、材质、细节、服饰、位置、朝向、数量和人物关系是否符合要求。
- 动作与动态：动作执行者、动作内容、交互、物理逻辑、速度、连续性、形体和结构保持是否正确。
- 镜头与时序：景别、视角、运镜方式、运镜幅度、镜头切换、分镜顺序、持续时间和叙事节奏是否符合要求。
- 场景与视觉表达：背景、环境氛围、风格、构图、透视、比例、光影、色彩、材质和清晰度是否正确。
- 文字与合规：字幕、拼写、屏幕文字、水印、logo、禁止内容和安全要求是否满足。
- 音频与声画关系：台词内容、说话人、音色、发音、音乐、音效、噪声、卡顿、音画同步和有无声音是否符合要求。
反馈中提到的问题不得遗漏或草率否定；反馈之外的问题只输出明确、可审计、与 user_prompt 或参考图约束直接相关的异常。

6. 问题说明与受控分类

6.1 先写问题说明

问题说明 必须是一段完整、具体、可核验的文字，至少包含：

- prompt 或参考图要求的预期；
- 生成音视频的实际表现；
- 两者之间的具体差异；
- 有证据时补充相关主体、动作、时间或画面区域。
禁止只复述用户反馈，禁止用“效果不好”“不符合要求”等空泛表述，禁止把问题类型当作问题说明。

6.2 再选择问题类型

每个问题只能从以下标签中选择一个最直接、最具体的类型：

- 物体属性错误
- 角色属性错误
- 实体数量错误
- 动作异常
- 表情异常
- 动态与动效问题
- 运镜方式问题
- 镜头变化问题
- 镜头语言问题
- 运镜幅度异常
- 场景设定问题
- 环境氛围问题
- 音频质量问题
- 风格错误
- 时序错误
- 结构不合理
- 透视效果异常
- 比例异常
- 动态结构保持异常
- 动作速率异常
- 色彩搭配问题
- 材质细节问题
- 构图结构问题
- 光影效果问题
- 叙事节奏问题
- 清晰度异常
- 稳定性异常
- 杂质噪点问题
- 文字质量问题
- 违规
- 主体id不一致
- 其他
归类规则：

- 不得输出组合标签、斜杠标签、带解释的标签或临时自造标签。
- 字幕、错别字、多余文字、错误屏幕文字归为 文字质量问题。
- 人物、商品或指定主体与参考身份不一致归为 主体id不一致。
- 身份正确但人物服装、发型等属性错误归为 角色属性错误；商品、道具等属性错误归为 物体属性错误。
- 动作内容或执行者错误归为 动作异常；动作快慢错误归为 动作速率异常。
- 运动过程中人物、商品或物体结构不能保持归为 动态结构保持异常。
- 不应发生的切镜、景别变化或镜头数量错误归为 镜头变化问题；运镜手法错误归为 运镜方式问题；推拉摇移幅度错误归为 运镜幅度异常。
- 音色、发音、台词、音乐、音效、杂音、卡顿或音画同步问题统一归为 音频质量问题。
- 只有在上述标签均不能准确表达问题核心时才选择 其他。
如果一条反馈或一次正常检查同时包含多个独立异常，应拆成多个问题点并分别归类。例如“人物身份错误且动作顺序错误”必须拆成 主体id不一致 和 时序错误 两条。

7. 可定位性与置信度

7.1 可定位性

- 是：异常有明确视觉主体或画面区域，可以用矩形 BBox 表示。全屏构图、场景、风格或镜头问题可使用全屏 BBox。
- 否：纯音频、纯主观评价、优化建议、功能或计费诉求、错误反馈、证据不足，或没有可对应的视觉区域。
- 部分：同一问题无法合理拆分，且同时包含可框选视觉证据和不可框选内容。能够拆分时优先拆分，不使用 部分。
一致性要求：

- 可定位性=是 或 部分：必须有关键帧和有效 BBox。
- 可定位性=否：关键帧秒和 BBox 必须为空字符串；若纯音频问题可确定发生时间，时间区间仍应填写。
7.2 置信度

- 高：prompt 或参考约束明确，生成结果证据清晰，结论几乎不依赖主观解释。
- 中：有可验证依据，但参考绑定、画面证据或审美标准存在有限歧义。
- 低：反馈主观、模糊、证据弱或资源不完整，无法形成稳定一致的判断。
置信度描述的是结论可靠性，不是问题严重程度。

8. 自适应音视频检查流程

8.1 准入与路由

先解析文本和参考绑定，再决定工具调用：

- 纯功能、计费、优化建议或泛化吐槽：反馈本身不抽帧、不作为可定位问题；仍按正常流程检查其他明确问题。
- 纯音频反馈：必须检查音轨、可播放音频或可靠转写；不得用静态画面证明声音问题。
- prompt 或反馈给出准确时间：直接检查该时间附近，可跳过全片粗筛。
- 时间未知的视觉问题：先做全片粗筛，再对候选区间细筛。
- 静态且在粗筛中已经清晰可见的问题：可跳过细筛，直接抽最终关键帧。
- 短闪、快速动作、身份漂移、结构崩坏或切镜问题：必须使用细粒度相邻帧检查。
8.2 视频元数据

对需要视觉证据的问题先用 ffprobe 获取时长、帧率、分辨率及音轨存在性。元数据只用于规划抽样，不能代替内容判断。

8.3 粗粒度检查

- 用一张接触表覆盖全片，通常包含 20-48 帧。
- 短视频建议 2 fps；超过 48 帧时降低 fps 或按时间分段。
- 一次查看接触表，优先记录与用户反馈最相关的候选时间；如同时发现其他明确异常，也记录 1-3 个最强候选时间。
- 接触表按从左到右、从上到下读取，时间映射为 起始秒 + (格序号 - 1) / fps。
8.4 细粒度检查

- 只围绕最强候选时间前后约 1-2 秒抽取，不重新遍历全片。
- 建议 6-12 fps、12-36 帧，用相邻帧确定异常开始、结束和峰值。
- 粗筛候选被否定时，最多补充一次粗筛；仍无证据则不得继续扩大搜索。
8.5 最终关键帧与 BBox

- 抽取异常最清晰的原分辨率或高清帧。
- 优先选择无遮挡、无裁切、无明显运动模糊、主体完整的帧。
- 禁止选择黑屏、白屏、转场、主体出框或无法证明问题的帧。
- 每个独立问题使用自己的关键帧；同一时间可以相同，但必须分别验证证据。
每个视频原则上控制在 2-4 次视频视觉输入：粗接触表、细接触表、最终关键帧，必要时再补一张局部放大图。该预算不包含调用方预先提供的参考图，但不得反复发送同一批参考图。

8.6 音频检查

涉及音频时必须实际检查音轨内容。可接受证据包括可播放音频、准确转写、带时间戳的声学分析或等价工具结果。仅知道“存在音轨”不能证明台词、音色、音效或同步正确。

如果执行环境只提供 ffprobe、接触表和静态关键帧，则无法可靠判断音频内容。此时不得编造音频结论，应报告证据不足；调用方需要补充音频播放、转写或分析能力。

音频证据来源必须显式区分：

- 直接音频：模型请求中实际包含从当前视频抽取的音轨，可以判断可听内容；是否支持仍以接口实际响应为准。
- ASR 工具证据：只能支持转写内容和近似时间，不能单独证明音色、杂音或口型同步。
- 无音频证据：不得输出台词、音色、杂音或音画同步结论。

8.7 专家工具与局部放大

- OCR、ASR、人脸、姿态、光流或音画同步工具的输出都是候选证据，不是问题必然存在的证明。
- 小区域缺陷可以在候选时间调用局部裁剪放大；裁剪范围必须来自原帧归一化 BBox，不能用放大图改变最终 BBox 的坐标系。
- 专家证据必须记录工具名、版本、适用性、观测值、时间/BBox、置信度和限制；缺少必要输入时标记为不适用，不得填造结果。
- 最终问题仍必须同时具备预期依据和生成结果证据。

9. 时间、关键帧与 BBox 标准

- 时间区间：填写问题首次出现到最后出现的区间，格式统一为 开始秒s - 结束秒s，例如 4s - 5s。
- 关键帧秒：填写最能证明视觉问题的单帧时间，使用不带单位的字符串，最多保留两位小数，例如 4.59。
- BBox：使用归一化 xyxy 坐标，顺序为左上角 (x1,y1) 到右下角 (x2,y2)，每个值范围为 [0,1]。
BBox 字符串统一格式：


框选规则：

- 人物局部异常只框异常局部，如手、脸、服装瑕疵或交互边界。
- 主体身份、全身动作或人物整体比例问题框对应主体整体。
- 商品、道具或屏幕问题框完整异常物体或屏幕区域。
- 字幕和文字问题只框异常文字区域。
- 全屏场景、风格、构图或镜头问题可用 <bbox>0,0,1,1</bbox>。
- BBox 必须包含能证明问题的最小必要证据，不得框入大量无关正常画面。
- 同一问题只输出一个 BBox；多个空间独立异常应拆成多个问题点。
纯音频或不可定位问题的 关键帧秒 和 BBox 统一填写空字符串 ""，不得填写 无、无需框选 或其他占位词。

10. 最终输出

输出必须是 JSON 数组，不使用 Markdown，不添加解释文字。没有可证实问题时输出空数组 `[]`；有问题时每个对象严格包含以下字段，字段顺序体现“先说明、后归类”：


纯音频问题示例：


若反馈经充分核查仍无法证实，且没有其他可证实问题需要输出，返回空数组 `[]`，不得输出“缺少证据”或“无法核实”的占位对象。

预测 CSV 必须保持输入列、100 条数据的行数和行顺序不变，仅在末尾新增 GPT预测结果 列。最终列名和顺序严格为：


GPT预测结果 单元格内容为上述 JSON 数组字符串。思考过程及标准答案 只能在预测完成后由执行器从原始行复制，不能参与 API 推理。

11. 离线评测真值

思考过程及标准答案 只供离线评测器使用。该字段中最后一个语言标记为 JSON 的 Markdown fenced code block 内的 JSON 数组，是该样本的结构化真值：

- <thinking>...</thinking> 和 fenced block 外文字不属于真值。
- 评测器必须提取 JSON 文本后使用标准 JSON 解析器解析。
- 缺少 fenced JSON、解析失败、顶层不是数组或字段不完整时，将样本标记为“真值无效”；不得从 thinking 文本补造答案。
- 比较前可做格式归一化：枚举值去除首尾空格；无、无需框选 等无值标记转换为空字符串；合法 BBox 解析为四个浮点数后比较。
- 推理阶段不得读取 fenced JSON、thinking 或它们的派生信息。
12. 输出前自检

- 是否优先、充分检查了用户反馈指向的问题？
- 是否正常补充了反馈之外的明确可证实问题，并排除了无关主观瑕疵？
- 是否查看了 prompt 引用的所有相关参考图，并正确绑定主体和属性？
- 是否同时给出了预期依据和生成结果证据？
- 音频结论是否来自真实音轨、转写或等价证据，而不是静态画面？
- 是否先写了一段具体问题说明，再选择唯一受控标签？
- 可定位性、时间区间、关键帧和 BBox 是否相互一致？
- 关键帧是否真正能证明问题，BBox 是否紧贴最小必要证据？
- 是否使用空字符串表示不可用字段，且没有输出 无 等占位词？
- 是否完全避免读取或泄漏标准答案？
