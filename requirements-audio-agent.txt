# Auralis parent-environment OCR dependencies.
# SenseVoice-Small + CAM++ run in the isolated Python environment declared in
# requirements-sensevoice.txt; the parent agent no longer uses large-v3.
# Python 3.10; verified in the `avagent` Conda environment on 2026-07-28.
rapidocr==3.9.2
onnxruntime==1.23.2
# The parent test/runtime environment imports AVBench's evaluator modules too.
# Keep pandas here as well as in third_party/AVBench/requirements.txt.
pandas==2.3.3
